--- docker-compose.yml
+++ docker-compose.yml
@@ -4,7 +4,7 @@ services:
   pixelspace-checker:
     container_name: pixelspace-checker
     build: .
-    command: bash -c 'uvicorn example:checker.app'
+#    command: bash -c 'uvicorn example:checker.app'
     ports:
       - 8000:8000
     environment:
@@ -13,8 +13,8 @@ services:
     - MONGO_PORT=27017
     - MONGO_USER=root 
     - MONGO_PASSWORD=root
-    logging:
-      driver: none
+#    logging:
+#      driver: none
 
 
   pixelspace-checker-db:
