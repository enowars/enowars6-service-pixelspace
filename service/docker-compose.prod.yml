version: '3.3'

services:
  web:
    container_name: Pixelspace
    build: 
      context: .
      dockerfile: Dockerfile.prod
    command: gunicorn pixelspace.asgi:application --bind 0.0.0.0:8088
    volumes:
      - media_volume:/mediafiles
    expose:
      - 8088
    env_file:
      - ./.env.prod
    depends_on:
      - db
  db:
    container_name: Pixelspace_db
    image: postgres:13.0-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ./.env.prod.db
  nginx:
    container_name: Pixelspace_rev_proxy
    build: ./nginx
    volumes:
      - static_volume:/staticfiles
      - media_volume:/mediafiles
    ports:
      - 8010:80
    depends_on:
      - web
volumes:
  postgres_data:
  static_volume:
  media_volume: